<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/cartFragment"
        android:title="Cart"
        android:icon="@drawable/ic_baseline_shopping_cart_24"
        app:showAsAction="always"
        app:actionLayout="@layout/cart_action_item"
        />

    <item android:id="@+id/orderActivity"
        android:title="Cart"
        android:icon="@drawable/ic_cart_white"
        app:showAsAction="always"
        app:actionLayout="@layout/order_action_item"
        />

    <item android:id="@+id/userActivity"
        android:title="Cart"
        android:icon="@drawable/user"
        app:showAsAction="always"
        app:actionLayout="@layout/user_action_item"
        />

</menu>